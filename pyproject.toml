[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "mls-projections"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["data_pipeline*", "models*", "scripts*", "features*", "config*"]
