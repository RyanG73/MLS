# MLS Dashboard — active build dependencies (F7)
#
# Covers only the active dashboard / model build path.
# Test + research tools:              requirements-dev.txt
# Legacy Pi / Postgres / Streamlit:   requirements-legacy.txt
#
# Python: 3.13 (see .python-version)

# ── Core data ────────────────────────────────────────────────────────────────
itscalledsoccer>=2.1.0,<3.0.0
understatapi>=0.7.1,<1.0.0
pandas>=2.0.0,<3.1.0
numpy>=1.26.0,<3.0.0
scipy>=1.12.0,<2.0.0
# Parquet engine — required by the odds/drift/source-health/football-data-intl
# reads+writes. Present locally as a transitive dep, but absent from a clean CI
# `pip install -r requirements.txt`, which broke every parquet-reading build step.
pyarrow>=14.0.0,<25.0.0

# ── Machine learning ─────────────────────────────────────────────────────────
xgboost>=2.0.0,<4.0.0
scikit-learn>=1.4.0,<2.0.0

# ── Market / statistics ──────────────────────────────────────────────────────
pyaml>=26.0.0,<27.0.0
penaltyblog>=1.6.2,<2.0.0

# ── Web / scraping ───────────────────────────────────────────────────────────
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=5.2.0,<7.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
haversine>=2.8.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
pyyaml>=6.0.1,<7.0.0
tqdm>=4.66.0,<5.0.0
tenacity>=8.3.0,<10.0.0

# ──────────────────────────────────────────────────────────────────────────────
# LOCKFILE GENERATION (run on the target machine):
#
#   make lock              # → writes requirements.lock via `pip freeze`
#   pip install -r requirements.lock   # reproducible install
#
# Regenerate requirements.lock whenever requirements.txt or
# requirements-dev.txt changes.
