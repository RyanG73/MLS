# MLS Prediction System — Configuration
#
# NOT the model config. This file used to say "all tunable parameters live here",
# and that stopped being true when the webapp/harness architecture replaced the
# Postgres/Streamlit stack in June 2026: the new code hardcodes its own values
# and 17 of the 18 sections here had zero readers. They were the archived stack's
# settings, and were deleted with it on 2026-08-04.
#
# Model constants (ELO K/HOME_ADV/REGRESS, DC half-life, xG windows, edge
# threshold) are canonical in CLAUDE.md "Key decisions" and docs/CURRENT_STATE.md.
# Editing them here would change nothing — which is why they are no longer here.
#
# What remains is what is actually read:
#   data   — config/__init__.py resolves current_season on load
#   market — data_pipeline/odds_log.py reads sport_key, regions, odds_format

# ─── Data ────────────────────────────────────────────────────────────────────
data:
  raw_data_dir: "data/raw"
  backfill_start_season: 2011          # ASA API reliable from 2011; earlier years sparse
  current_season: null                 # Optional; defaults to current year or env MLS_CURRENT_SEASON

# ─── Market / Betting ────────────────────────────────────────────────────────
market:
  bookmaker: "pinnacle"
  sport_key: "soccer_usa_mls"          # The Odds API sport key
  regions: "us"
  markets: "h2h"                        # 1X2 head-to-head
  odds_format: "decimal"
  default_edge_threshold_pct: 8.0     # Default value-bet cutoff — validated 2026 (CLAUDE.md: 8%)
  max_edge_threshold_pct: 20.0        # Upper bound for slider controls in dashboard
  kelly_fractions: [0.25, 0.50]
  starting_bankroll: 10000             # Simulated bankroll in units
  # The-Odds-API sport keys for covered leagues (B10). Filed, NOT fetched:
  # free tier is 500 req/mo; MLS openers+closers ≈ 60 req/mo. Enabling all
  # leagues below at daily cadence ≈ 390 more — fits, but leaves no headroom
  # for closers timing; paid key deferred (user decision 2026-07-03) until
  # B5/B10/Part C are live.
  league_sport_keys:
    mls: "soccer_usa_mls"
    epl: "soccer_epl"
    la-liga: "soccer_spain_la_liga"
    serie-a: "soccer_italy_serie_a"
    bundesliga: "soccer_germany_bundesliga"
    ligue-1: "soccer_france_ligue_one"
    championship: "soccer_efl_champ"
    league-one: "soccer_england_league1"
    league-two: "soccer_england_league2"
    segunda: "soccer_spain_segunda_division"
    serie-b: "soccer_italy_serie_b"
    bundesliga-2: "soccer_germany_bundesliga2"
    ligue-2: "soccer_france_ligue_two"
    liga-mx: "soccer_mexico_ligamx"
